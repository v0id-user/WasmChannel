{
  "name": "worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@orpc/client": "^1.3.0",
    "@orpc/server": "^1.3.0",
    "better-auth": "^1.2.8",
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}